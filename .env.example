# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=fastify_db
DB_PORT=3306

# JWT Secret (Generate a secure one: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your-secret-key-change-in-production

# Server Configuration
PORT=3000
